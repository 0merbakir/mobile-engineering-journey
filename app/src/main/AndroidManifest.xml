<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Roadmaps">

        <!-- The one and only Activity, hosting all fragments. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Day 3 Activities (no longer launched directly) -->
        <activity android:name=".day3.FirstActivity" />
        <activity android:name=".day3.SecondActivity" />

        <!-- Day 4 Activities (no longer used) -->
        <activity android:name=".day4.ListActivity" />
        <activity android:name=".day4.DetailActivity" android:parentActivityName=".day4.ListActivity" />

    </application>

</manifest>